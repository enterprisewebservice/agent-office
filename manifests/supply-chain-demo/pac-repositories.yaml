# Pipelines-as-Code onboarding for supply-chain-demo repos — DECLARATIVE.
# A Repository CR is all PaC needs to accept webhooks for a repo (the org-wide
# GitHub App delivers them); the repo itself carries its .tekton/ pipeline.
# This is the mechanism forge-dev's golden-path template relies on: publish the
# repo + this CR lands via gitops -> first push builds, no console clicks.
apiVersion: pipelinesascode.tekton.dev/v1alpha1
kind: Repository
metadata:
  name: konflux-onboard-spike
  namespace: default-tenant
  labels:
    app.kubernetes.io/part-of: supply-chain-demo
spec:
  url: https://github.com/enterprisewebservice/konflux-onboard-spike
---
# golden-path-probe — inc-3 rails probe (template-rendered repo, untouched by hands)
apiVersion: pipelinesascode.tekton.dev/v1alpha1
kind: Repository
metadata:
  name: golden-path-probe
  namespace: default-tenant
spec:
  url: https://github.com/enterprisewebservice/golden-path-probe
---
# ops-metrics — pre-staged rails for the supply-chain scenario centerpiece:
# forge-dev creates + pushes the repo; this CR routes its PaC webhooks to
# default-tenant the moment the first push lands. (Repo may not exist yet —
# the CR just waits.)
apiVersion: pipelinesascode.tekton.dev/v1alpha1
kind: Repository
metadata:
  name: ops-metrics
  namespace: default-tenant
spec:
  url: https://github.com/enterprisewebservice/ops-metrics
