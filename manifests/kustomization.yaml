apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Mirrors the previous Application source: `directory: { recurse: true, exclude: gitops/* }`.
# Switching to kustomize so Argo CD Image Updater can edit the `images:` block
# below on every successful Tekton build (rebumps `newTag` to the newest
# `on-push-<sha>` Quay tag, commits to main, ArgoCD reconciles, pod rolls).
#
# CRDs are managed by the OLM-installed agent-office-operator
# bundle now (not raw kustomize) — the prior `crd/` ref was
# dangling after CRD lifecycle moved to the operator.
#
# onboarding-agent.yaml sample is also removed: the agent was
# deleted earlier in the platform's lifetime and leaving the
# sample in this kustomize root would resurrect it on every
# Argo sync.
resources:
  - deploy/agent-office-deploy.yaml
  - rbac/agent-office-rbac.yaml
  # supply-chain demo scenery: the pre-existing endpoint the ops manager exports CSVs from
  - supply-chain-demo/orders-api/orders-app.yaml
  - supply-chain-demo/orders-api/orders-data.yaml
  # supply-chain demo: declarative PaC onboarding (new repos build hands-free)
  - supply-chain-demo/pac-repositories.yaml
# Image source-of-truth. Change `newTag` here to deploy a different version;
# Image Updater rewrites it automatically when a new build lands.
images:
  - name: quay-quay-quay-test.apps.salamander.aimlworkbench.com/deanpeterson/agent-office-server
    newTag: on-push-85d631be760ee308f1c3217b68f6a945bccee3e9
