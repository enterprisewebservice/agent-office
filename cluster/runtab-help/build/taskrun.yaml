apiVersion: tekton.dev/v1
kind: TaskRun
metadata:
  generateName: runtab-support-mcp-build-
  namespace: agent-office
spec:
  taskRef:
    name: runtab-support-mcp-build
  serviceAccountName: pipeline
  workspaces:
    - name: dockerconfig
      secret:
        secretName: quay-push-secret
        items:
          - key: .dockerconfigjson
            path: .dockerconfigjson
    - name: source
      emptyDir: {}
