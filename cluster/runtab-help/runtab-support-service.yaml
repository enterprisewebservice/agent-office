apiVersion: v1
kind: Service
metadata:
  name: runtab-support-mcp
  labels:
    app.kubernetes.io/name: runtab-support-mcp
    app.kubernetes.io/managed-by: agent-office-gitops
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: runtab-support-mcp
  ports:
    - name: mcp
      port: 8080
      targetPort: mcp
      protocol: TCP
